/* ============================================================
   config.js — конфиг игры, значения которые основатель может
   поменять без правки логики.

   vkOrderVerifyUrl: сервер, который сверяет статус заказа ВК-инапа
   (задача 2, ИНАП «Мятная бумага»). VKWebAppShowOrderBox возвращает
   статус СИНХРОННО с клиента — он НЕ подтверждает реальное списание
   (клиент можно подделать/эмулировать). Сервер получает уведомление
   от ВК напрямую (см. dev.vk.com/ru/api/payments/notifications) и
   может авторитетно ответить "товар куплен да/нет".

   Пусто = сервер ещё не поднят. adapters/vk_bridge.js в этом случае
   считает покупку НЕ подтверждённой (честная ошибка игроку, не ложный
   успех) — так же, как если бы сервер не ответил. Впишите реальный
   URL, когда сервер будет готов; в dev-режиме (не в VK-приложении)
   сервер не нужен — там покупка эмулируется локально, см. adapter.
   ============================================================ */
window.GAME_CONFIG = {
  vkOrderVerifyUrl: '', // TODO(основатель): вписать URL после поднятия сервера
};
